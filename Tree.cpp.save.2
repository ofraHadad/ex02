#include <iostream>
using namespace std;

#include "Tree.h"
#include "Node.h"




Tree :: Tree() 
{
	head=NULL;
}

Node* Tree::getRoot()
{
	return this -> head;
}

void  Tree::insert(int x)
{
	
	if(getRoot()==NULL)
	{
		head= new Node(x);
		count=1;
	}
	else if(getRoot()->getNum()>x)
	{
		insert(x,&(getRoot()->left),getRoot());
	}
	else
	{
 		insert(x,&(getRoot()->right),getRoot());
	}
}

void Tree :: insert(int x ,Node** n,Node* p)
{
 Node * pp= *n;
	if (pp==NULL)
	{
		Node temp(x);
		*n=&temp;
		
		cout<<pp->getNum()<<"/n";
		pp->setParent(p);
		count++;

	}
	else if(pp->getNum()>x)
	{
	cout<<"ttt";
		insert(x,&(pp->getLeft()),n);
	}
	else
	
	{
	cout<<"555";
		insert(x,&(pp->getRight()),n);
	}
}

void Tree :: print()	
{
	printInorder(getRoot());
	cout<<"\n"<< count;
}

void Tree :: printInorder( Node* node) 
{ 
    if (node == NULL) 
        return; 
  
    /* first recur on left child */
    printInorder(node->getLeft()); 
  
    /* then print the data of node */
    cout << node->getNum() << "."; 
  
    /* now recur on right child */
    printInorder(node-> getRight()); 
}

int Tree::size()
{
	return count;
}

int Tree:: root()
{//add hariga null
	return getRoot()->getNum();
}

int Tree :: parent(int x)
{
	Node * n=search(x,getRoot());
	if(n==NULL || n->getParent()==NULL)
	{
	//hrigaa
	return 10;
	}
	
	return n->getParent()->getNum();
	
}

int Tree:: contains(int x)// 1 false  0 true
{
	Node * n= search(x,getRoot());
	if(n==NULL)
	{
		return 1;
	}
	return 0;
}

int Tree :: left(int x)
{
	cout<<"44";
	Node * n= search(x,getRoot());
	cout<<"66";
	if (n==NULL)
	{
//harigaaa
	}
	if(n->getLeft()==NULL)
	{
//harigaa
	}
	return n->getLeft()->getNum();
}

int Tree :: right(int x)
{
        Node * n= search(x,getRoot());
        if (n==NULL)
	{
//harigaaa
	}
        if(n->getRight()==NULL)
	{
//harigaa
	}
        return n->getRight()->getNum();
}

Node * Tree::search(int x, Node* n)
{
	if(n==NULL)
	{
		return n;
	}
	if (n->getNum()==x)
	{
		return n;
	}
	if(n->getNum()>x)
	{
		return search(x,n->getLeft());
	}
 	else
	{
		return search(x,n->getRight());
	}
}
